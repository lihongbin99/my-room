# 依赖与构建产物
node_modules/
dist/

# 下载的原始模型（大文件，裁剪后的在 public/models/）
models-src/

# 克隆的参考仓库（分析用，勿修改）
reference/

# NES ROM（版权原因不入库，自备后放这里）
public/roms/*.nes

# 已迁 OSS 的大资源（本地只是 crush/上传的中间产物，运行时从 OSS 加载）
public/models/
public/books/
public/paintings/

# OSS 密钥（upload-oss.mjs 环境变量缺失时自动读它，绝不能提交）
.env.oss

# 编辑器/系统
.DS_Store
Thumbs.db
